app:
  debug: false
modules:
  core:
    server:
      metrics:
        collect:
          enabled: false
      debug:
        pprof:
          expose: false
        stats:
          expose: false
  log:
    level: info
    output: stdout
